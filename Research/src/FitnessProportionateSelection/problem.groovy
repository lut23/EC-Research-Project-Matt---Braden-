package FitnessProportionateSelection
import java.util.Random 

class problem {

    def testPop(){
       def pop
       return pop
        
    }
    public quality(candidate){
        candidate
    }
    public pop(popsize){
        def rand = new Random()
        def pop = []
       
        for(i in 0..<popsize){
            pop.add( rand.nextInt(100))
        }
        return pop
       
    }
}
